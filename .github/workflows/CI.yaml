name: "CI"
on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Build"
        run: bazel build //...

      - name: "Run tests"
        run: bazel test //...
